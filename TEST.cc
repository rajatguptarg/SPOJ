/*
 * USER: rajatguptarg
 * TASK: TEST
 * ALGO: ad-hoc
 */
 
#include <bits/stdc++.h>

using namespace std;

int main(int argc, char *argv[]) {
    int num;
    scanf("%d", &num);
    while(num != 42) {
        printf("%d\n", num);
        scanf("%d", &num);
    }
    return 0;
}

